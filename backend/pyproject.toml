[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.135.1",
    "uvicorn==0.41.0",
#    "pytest==9.0.2",  # CVE-2025-71176可能会在CI/CD中被利用。但这就是目前最新版，需要等待官方修复。暂时禁用这个模块。
    "psutil==7.2.2",
]
